AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=
# Optional: override the Azure Service Bus queue used for email send/receive.
# Default: email-queue
SERVICE_BUS_EMAIL_QUEUE=

# Optional: override the Postgres database name. Default: lindabi
DATABASE=
DATABASE_HOST=
DATABASE_PASSWORD=

# Optional: override the HTTP port the API listens on. Default: 3800
PORT=